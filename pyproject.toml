[tool.poetry]
name = "shapegame"
version = "0.1.0"
description = ""
authors = ["Rafal Nowicki <rafal.nowicki@kontomatik.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pygame = "^2.5"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
